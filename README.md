# Sovereign luxury watch site

A responsive, scroll-driven product story built from the 240 supplied JPG frames.

## Run locally

From this directory:

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.

The frame sequence lives in `assets/frames/` and is progressively preloaded by `script.js`.
# 3d-website
